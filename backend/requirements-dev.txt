# Development/test-only dependencies (installed into the api image for in-container
# testing and linting). Runtime deps live in requirements.txt.
-r requirements.txt
pytest>=8.2
pytest-durations>=1.2
ruff>=0.5
