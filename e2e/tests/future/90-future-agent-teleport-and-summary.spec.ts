import { test } from '@playwright/test';

test.describe.skip('future @slow @safety — agent teleport and scoped summary acceptance', () => {
  test('agent index-only paper can be requested, approved, teleported, extracted, and summarized', async () => {
    throw new Error('Future acceptance test placeholder.');
  });

  test('shelf insights page explains missing foundational references and citation contexts', async () => {
    throw new Error('Future acceptance test placeholder.');
  });
});
