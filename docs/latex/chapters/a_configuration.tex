\section{Configuration Appendix}

Configuration examples live in \texttt{config/server.example.yaml} and \texttt{config/agent.example.yaml}. Local copies should use \texttt{*.local.yaml} and must not be committed.

For LAN access, bind the public API only after authentication is implemented and internal services remain private.
