# Handoff — issue batch 10 (2026-07-10)

Five owner-requested features. Plan: `docs/WORKPLAN_2026-07-10_batch10.md` (design forks resolved
by the owner and recorded inline in the plan). On `main`, not pushed. One commit per logical chunk.

## Feature 3 — "Set metadata from best source" → "all fields" ✅

- **Files changed:**
  - `backend/app/api/v1/endpoints/works.py` — extracted the per-field promotion into
    `_promote_best_field(db, work, field_name) -> bool`; `bulk_apply_best_metadata` accepts
    `field_name == "all"` (expands to sorted `_PROMOTABLE_FIELDS`) and loops, counting a paper
    "applied" if ≥1 field was promoted.
  - `frontend/src/pages/LibraryPage.svelte` — "all fields" option in the field picker + toast text.
  - `frontend/src/api/client.ts` — no signature change (already `fieldName: string`).
- **Assumptions:** "all" means all promotable Work columns (title/abstract/year/venue/doi);
  authors/keywords/topics are not promotable via this path.
- **Tests:** `backend/tests/test_metadata_assertion_delete.py::test_bulk_apply_all_promotes_every_field`
  (locked-field skip, multi-field promote, per-paper applied/skipped tally);
  `frontend/src/pages/LibraryPage.batch.test.ts` "all fields" case. Both suites green.
- **Security:** locked/`confirmed_fields` still skipped per-field; audit event `metadata.bulk_applied`
  unchanged (records `field: "all"`).
- **Next:** features 4, 2, 5, 1.

## Feature 4 — editable authors in Details ✅

- **Files changed:**
  - `backend/app/api/v1/endpoints/works.py` — new `SetMetadataValue` model + `POST
    /{work_id}/metadata/set` (`set_metadata_value`): writes a `source="user"` canonical assertion,
    locks the field (`confirmed_fields`), applies to the Work column for promotable fields, empty
    value clears + unlocks. Emits `metadata.value_set` audit event. Returns the refreshed
    `list[FieldReview]`.
  - `frontend/src/api/client.ts` — `setMetadataValue(workId, fieldName, value)`.
  - `frontend/src/components/WorkDetail.svelte` — `authorsFromFields()` helper (shared with
    `searchedAuthors`); `form.authors` seeded after metadata load; editable Authors input in the
    Details panel; `save()` calls `setMetadataValue` only when authors changed.
  - `backend/openapi.json` regenerated.
- **Assumptions:** authors is a `;`-separated free-text string (no structured author model).
- **Tests:** `test_metadata_assertion_delete.py` — set creates locked user assertion, promotable
  field updates Work, empty clears+unlocks. `WorkDetail.metadata.test.ts` — seeds + persists change,
  no-op when unchanged. Both green.
- **Security:** modify-guard on the endpoint; manual value locked so enrichment won't clobber it.
## Feature 2 — duplicates preview + open papers ✅

- **Files changed:**
  - `frontend/src/api/client.ts` — `getMergePreview` now passes `{ timeoutMs: 15000 }`.
  - `frontend/src/pages/DuplicatesPage.svelte` — import `Modal` + `WorkDetail` + `Work`; `detailWork`
    state + `openInPaperView(workId)`; base/source labels are `.paper-link` buttons that open the
    modal; `mergeSummary(null)` returns an explicit "Preview unavailable…" note; modal appended.
  - `frontend/src/pages/DuplicatesPage.test.ts` — mock `getWork` + WorkDetail load calls; tests for
    open-in-paper-view and preview-error note.
- **Assumptions:** the paper view is read/compare only here; merge target is still chosen via the
  existing `⇄` swap. Reused the exact CitationSummaryPage modal pattern.
- **Tests:** DuplicatesPage.test.ts (8 tests) green.
- **Security:** no new endpoint; getWork is SEE-guarded server-side as before.
## Feature 5 — new library columns ✅

- **Files changed:**
  - `backend/app/api/v1/endpoints/works.py` — `WorkTagRef` schema; `WorkRead` gains
    `file_count`/`tags`/`badges` (+ none-to-list validator); `_batch_library_columns(db, works)`
    (files→count+status+text-layer, tags via TagLink, conflict probe over metadata_assertions);
    wired into `list_works`.
  - `backend/openapi.json` regenerated.
  - `frontend/src/api/client.ts` — `Work.file_count/tags/badges` + `WorkTagRef`.
  - `frontend/src/lib/columns.ts` — 4 new `ColumnId`s + registry entries (all `default:false`);
    `normalizeColumnPrefs` already auto-appends them for existing users.
  - `frontend/src/components/PaperTable.svelte` — render branches + `badgeMeta` token→chip mapping +
    badge/tag/num styles (reuses `--status-*` tokens).
- **Assumptions:** badges are UI-agnostic tokens; unknown tokens degrade to a neutral chip. Badge
  set is aggregated over a work's files + a conflict probe (any field with ≥2 distinct values).
- **Tests:** `backend/tests/test_library_columns.py` (file_count/tags/badges/conflict/stub);
  `test_m1_core_library.py` fixture updated to create `metadata_assertions` (list_works now reads it);
  `frontend/src/components/PaperTable.test.ts` (render + placeholders). All green.
- **Security:** tags/badges derive from already-SEE-filtered works on the page; no new endpoint.
## Feature 1 — multi-PDF staging import _(pending)_
