import { describe, expect, it } from 'vitest';

describe.skip('future literature-review workflows', () => {
  it('shows a scoped shelf citation-summary dashboard with missing references', () => {
    expect.fail('Future acceptance test placeholder.');
  });

  it('explains agent index-only vs teleport decisions in the UI', () => {
    expect.fail('Future acceptance test placeholder.');
  });

  it('shows local-AI summary provenance and fallback reason in paper detail', () => {
    expect.fail('Future acceptance test placeholder.');
  });
});
