// Explicit (default) Svelte config. Everything here matches vite-plugin-svelte's defaults — the
// file exists so builds don't log "no Svelte config found … using default configuration" on every
// run. Add real compiler/preprocess options here if they're ever needed.
export default {};
